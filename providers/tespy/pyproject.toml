[project]
name = "forge-tespy-provider"
version = "0.1.0"
description = "TESPy thermal-fluid network provider for Forge (crafty). Computes; holds no scientific authority."
requires-python = ">=3.10"
dependencies = ["crafty", "tespy>=0.11"]

[tool.setuptools]
packages = ["forge_tespy"]
