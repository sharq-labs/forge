[project]
name = "forge-pybamm-provider"
version = "0.1.0"
description = "PyBaMM battery provider for Forge (crafty). Computes; holds no scientific authority."
requires-python = ">=3.10"
dependencies = ["crafty", "pybamm>=26"]

[tool.setuptools]
packages = ["forge_pybamm"]
