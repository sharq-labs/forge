name: Tests

# FAST tier on every supported Python for every push and PR.
# SCIENTIFIC is also a PR gate (Python 3.12) so model/solver regressions are
# actionable before merge. FULL campaign tests remain a manual freeze check.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: tests-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  repo-layout:
    name: repository layout
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Enforce documentation layout
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          root_markdown = sorted(path.name for path in Path('.').glob('*.md'))
          allowed_root = {'README.md'}
          unexpected_root = [name for name in root_markdown if name not in allowed_root]
          source_markdown = sorted(str(path) for path in Path('src').rglob('*.md'))

          problems = []
          if unexpected_root:
              problems.append(
                  'long-form Markdown belongs under docs/; unexpected root files: '
                  + ', '.join(unexpected_root)
              )
          if source_markdown:
              problems.append(
                  'src/ is code/docstrings only; move Markdown documentation out of src/: '
                  + ', '.join(source_markdown)
              )

          if problems:
              raise SystemExit('\n'.join(problems))

          print('documentation layout OK: root entry point is README.md and src/ has no Markdown')
          PY

  fast:
    name: fast (py ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      # Freeze/certificate checks ask whether historical candidate commits are
      # ancestors of HEAD. A depth-1 checkout makes every older candidate look
      # UNRELATED even when the repository relation is valid.
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      # A core-changing PR source commit necessarily carries the previous
      # certificate until Recertify Hardened Core finishes its measured gates
      # and pushes a certificate-only child. Defer only the four self-checks on
      # such a source commit; ordinary PRs and the exact certificate child must
      # run them normally.
      - name: Classify certificate-check phase
        id: certificate_phase
        shell: bash
        run: |
          set -euo pipefail
          run_checks=true

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            head_sha="${{ github.event.pull_request.head.sha }}"
            base_sha="${{ github.event.pull_request.base.sha }}"
            subject="$(git log -1 --format=%s "$head_sha")"

            if [ "$subject" = "certification: recertify hardened core" ]; then
              parent_count="$(git rev-list --parents -n 1 "$head_sha" | awk '{print NF-1}')"
              if [ "$parent_count" -ne 1 ]; then
                echo "A certificate child must have exactly one parent; found $parent_count."
                exit 1
              fi
              parent_sha="$(git rev-parse "$head_sha^")"
              mapfile -t changed < <(git diff --name-only "$parent_sha" "$head_sha")
              if [ "${#changed[@]}" -ne 1 ] || [ "${changed[0]}" != "certification/current_core_v2.json" ]; then
                echo "The recertification subject is reserved for a certificate-only child commit."
                printf 'Changed path: %s\n' "${changed[@]:-<none>}"
                exit 1
              fi
              echo "Certificate-only child detected: full certificate/freeze checks stay enabled."
            else
              recertification_needed=false
              while IFS= read -r path; do
                case "$path" in
                  .github/workflows/recertify-hardened-core.yml|src/engcore/scientific/*|src/engcore/execution/*|src/engcore/extensions.py|src/engcore/uq/cross_domain.py|tests/*|benchmarks/trust_hardening/*)
                    recertification_needed=true
                    break
                    ;;
                esac
              done < <(git diff --name-only "$base_sha" "$head_sha")

              if [ "$recertification_needed" = "true" ]; then
                run_checks=false
                echo "Recertified-core PR source detected: deferring four certificate/freeze self-checks to the certificate child."
              else
                echo "Ordinary PR detected: full certificate/freeze checks stay enabled."
              fi
            fi
          else
            echo "Non-PR run: full certificate/freeze checks stay enabled."
          fi

          echo "run_checks=$run_checks" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python-version }}
      # FAST collects MCP and scientific-truth tests, so both optional groups
      # must be present. The latter hard-imports mpmath during collection.
      - run: pip install -e ".[dev,mcp,oracles]"
      # Guard 19 walks live repository Python files. Blind-v2 deliberately
      # creates then deletes a falsification module inside its frozen package.
      # Running the guard concurrently with that writer makes the filesystem
      # population change between rglob() and read_text(). Keep the guard fully
      # strict, but execute it alone before the parallel suite.
      - name: Dependency reachability guard (serial)
        run: >-
          python -m pytest
          tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared
          -q
      # -n 4, not -n auto: the worker count must not track runner hardware.
      - name: FAST suite
        shell: bash
        run: |
          set -euo pipefail
          certificate_args=()
          if [ "${{ steps.certificate_phase.outputs.run_checks }}" != "true" ]; then
            certificate_args+=(
              --deselect tests/test_core_certificate.py::test_the_certificate_describes_this_tree
              --deselect tests/test_core_certificate.py::test_the_certificate_records_the_v1_relationship_truthfully
              --deselect tests/test_core_freeze_manifest.py::test_the_tree_is_core_freeze_v1
              --deselect tests/test_core_freeze_manifest.py::test_a_descendant_that_keeps_the_contract_still_verifies
            )
          fi
          python -m pytest -m "not expensive" -q -n 4 --dist loadfile \
            --deselect tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared \
            "${certificate_args[@]}"

  # Mutation guards stay on one interpreter: they verify the guards themselves,
  # not interpreter compatibility. Running the whole mutation campaign twice
  # would add cost without testing a different contract.
  mutations:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: pip install -e ".[dev,mcp,oracles]"
      - run: python -X utf8 tests/mutation_guards.py "${RUNNER_TEMP}/mutations"

  # This is intentionally a PR gate now. A scientific regression found only
  # after merge is evidence discovered one boundary too late. Keep it on 3.12
  # because FAST owns the supported-Python matrix; this job owns the scientific
  # execution/provider contract.
  scientific:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Classify certificate-check phase
        id: certificate_phase
        shell: bash
        run: |
          set -euo pipefail
          run_checks=true

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            head_sha="${{ github.event.pull_request.head.sha }}"
            base_sha="${{ github.event.pull_request.base.sha }}"
            subject="$(git log -1 --format=%s "$head_sha")"

            if [ "$subject" = "certification: recertify hardened core" ]; then
              parent_count="$(git rev-list --parents -n 1 "$head_sha" | awk '{print NF-1}')"
              if [ "$parent_count" -ne 1 ]; then
                echo "A certificate child must have exactly one parent; found $parent_count."
                exit 1
              fi
              parent_sha="$(git rev-parse "$head_sha^")"
              mapfile -t changed < <(git diff --name-only "$parent_sha" "$head_sha")
              if [ "${#changed[@]}" -ne 1 ] || [ "${changed[0]}" != "certification/current_core_v2.json" ]; then
                echo "The recertification subject is reserved for a certificate-only child commit."
                printf 'Changed path: %s\n' "${changed[@]:-<none>}"
                exit 1
              fi
              echo "Certificate-only child detected: full certificate/freeze checks stay enabled."
            else
              recertification_needed=false
              while IFS= read -r path; do
                case "$path" in
                  .github/workflows/recertify-hardened-core.yml|src/engcore/scientific/*|src/engcore/execution/*|src/engcore/extensions.py|src/engcore/uq/cross_domain.py|tests/*|benchmarks/trust_hardening/*)
                    recertification_needed=true
                    break
                    ;;
                esac
              done < <(git diff --name-only "$base_sha" "$head_sha")

              if [ "$recertification_needed" = "true" ]; then
                run_checks=false
                echo "Recertified-core PR source detected: deferring four certificate/freeze self-checks to the certificate child."
              else
                echo "Ordinary PR detected: full certificate/freeze checks stay enabled."
              fi
            fi
          else
            echo "Non-PR run: full certificate/freeze checks stay enabled."
          fi

          echo "run_checks=$run_checks" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: sudo apt-get install -y -qq ngspice
      - run: pip install -e ".[dev,mcp,oracles]"
      - name: Scientific suite
        shell: bash
        env:
          CRAFTY_NGSPICE_ARGV: ngspice
        run: |
          set -euo pipefail
          certificate_args=()
          if [ "${{ steps.certificate_phase.outputs.run_checks }}" != "true" ]; then
            certificate_args+=(
              --deselect tests/test_core_certificate.py::test_the_certificate_describes_this_tree
              --deselect tests/test_core_certificate.py::test_the_certificate_records_the_v1_relationship_truthfully
              --deselect tests/test_core_freeze_manifest.py::test_the_tree_is_core_freeze_v1
              --deselect tests/test_core_freeze_manifest.py::test_a_descendant_that_keeps_the_contract_still_verifies
            )
          fi
          python -m pytest -m "not campaign" -q -n 4 --dist loadfile \
            --deselect tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared \
            --deselect tests/test_heterogeneous_ngspice.py::test_a_the_provider_is_a_real_external_process \
            --deselect tests/test_heterogeneous_ngspice.py::test_g2_a_genuine_non_zero_exit_is_a_provider_failure \
            --deselect tests/test_heterogeneous_ngspice.py::test_j_relocating_the_provider_leaves_the_result_byte_identical \
            --deselect benchmarks/model_measurement_validation/tests/test_model_measurement_validation.py::test_rebuilding_everything_reproduces_the_published_gates \
            --deselect benchmarks/empirical_validation/tests/test_empirical_validation.py::test_rebuilding_everything_reproduces_the_published_gates \
            "${certificate_args[@]}"
      # The two benchmark rebuild nodes above are historical campaign
      # reproduction tests: each rebuilds/writes its round's committed artifacts
      # and explicitly asks whether *that old round* changed no production code.
      # Any real source-changing PR must fail that historical premise and the
      # write makes the checkout dirty, cascading into certificate failures.
      # Their non-mutating provenance, independence, falsification and committed
      # gate checks still run here; the full rebuilds remain available in the
      # campaign/manual workflow where their historical baseline is meaningful.

  # Bare-image reproducibility is still a post-merge/manual check. It is not a
  # scientific-code gate and is substantially more expensive than FAST.
  reproduce:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: docker build -t crafty-repro .
      - run: docker run --rm crafty-repro

  # The benchmark is scored on the DEVELOPMENT split only. The 600-case
  # hold-out stays sealed; score_hard.py exits non-zero if this job ever tries
  # to score it without --open-holdout.
  benchmark:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: pip install -e ".[dev]"
      - name: split is reproducible from the seed and the rule
        run: |
          python benchmarks/hard/split_hard.py --cases benchmarks/hard/cases_hard \
            --out /tmp/split_check.json
          python - <<'EOF'
          import json
          a = json.load(open("benchmarks/hard/split_hard.json"))
          b = json.load(open("/tmp/split_check.json"))
          for k in ("rule_id", "seed", "case_set_digest", "dev_digest", "holdout_digest"):
              assert a[k] == b[k], (k, a[k], b[k])
          print("split reproduced:", a["n_dev"], "dev +", a["n_holdout"], "hold-out")
          EOF
      - name: the seal refuses the full set without --open-holdout
        run: |
          if python benchmarks/hard/score_hard.py --src src \
               --cases benchmarks/hard/cases_hard --workers 4 --split all; then
            echo "SEAL BROKEN: --split all succeeded without --open-holdout"; exit 1
          fi
      - name: the committed benchmark records still reproduce
        run: python -m pytest tests/test_benchmark_records.py -q
