name: Recertify Hardened Core

on:
  pull_request:
    paths:
      - ".github/workflows/recertify-hardened-core.yml"
      - ".github/workflows/tests.yml"
      - "certification/current_core_v2.json"
      - "src/engcore/scientific/**"
      - "src/engcore/execution/**"
      - "src/engcore/extensions.py"
      - "src/engcore/uq/cross_domain.py"
      - "tests/**"
      - "benchmarks/trust_hardening/**"

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: recertify-${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  classify:
    runs-on: ubuntu-latest
    outputs:
      mode: ${{ steps.gate.outputs.mode }}
      source_sha: ${{ steps.gate.outputs.source_sha }}
      certificate_sha: ${{ steps.gate.outputs.certificate_sha }}
    steps:
      - name: Checkout exact pull-request head
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Classify source commit versus certificate child
        id: gate
        shell: bash
        run: |
          set -euo pipefail
          subject="$(git log -1 --format=%s)"
          head_sha="$(git rev-parse HEAD)"

          if [ "$subject" = "certification: recertify hardened core" ]; then
            parent_count="$(git rev-list --parents -n 1 HEAD | awk '{print NF-1}')"
            if [ "$parent_count" -ne 1 ]; then
              echo "A certificate child must have exactly one parent; found $parent_count."
              exit 1
            fi
            parent_sha="$(git rev-parse HEAD^)"
            mapfile -t changed < <(git diff --name-only "$parent_sha" HEAD)
            if [ "${#changed[@]}" -ne 1 ] || [ "${changed[0]}" != "certification/current_core_v2.json" ]; then
              echo "The recertification subject is reserved for a certificate-only child commit."
              printf 'Changed path: %s\n' "${changed[@]:-<none>}"
              exit 1
            fi
            echo "mode=certificate_child" >> "$GITHUB_OUTPUT"
            echo "source_sha=$parent_sha" >> "$GITHUB_OUTPUT"
            echo "certificate_sha=$head_sha" >> "$GITHUB_OUTPUT"
            echo "Verified certificate-only child shape for source $parent_sha."
          else
            echo "mode=source" >> "$GITHUB_OUTPUT"
            echo "source_sha=$head_sha" >> "$GITHUB_OUTPUT"
            echo "certificate_sha=" >> "$GITHUB_OUTPUT"
            echo "Source commit selected for parallel recertification: $head_sha"
          fi

  fast311:
    needs: classify
    if: needs.classify.outputs.mode == 'source'
    runs-on: ubuntu-latest
    outputs:
      python_version: ${{ steps.env.outputs.python_version }}
      pip_freeze_sha256: ${{ steps.env.outputs.pip_freeze_sha256 }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.classify.outputs.source_sha }}
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"
      - run: python -m pip install -e ".[dev,mcp,oracles]"
      - name: Dependency reachability guard — Python 3.11 (serial)
        run: >-
          python -m pytest
          tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared
          -q
      - name: FAST functional gate — Python 3.11
        run: >-
          python -m pytest -m "not expensive" -q -n 4 --dist loadfile
          --deselect tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared
          --deselect tests/test_core_certificate.py::test_the_certificate_describes_this_tree
          --deselect tests/test_core_certificate.py::test_the_certificate_records_the_v1_relationship_truthfully
          --deselect tests/test_core_freeze_manifest.py::test_the_tree_is_core_freeze_v1
          --deselect tests/test_core_freeze_manifest.py::test_a_descendant_that_keeps_the_contract_still_verifies
      - name: Record Python 3.11 environment
        id: env
        shell: bash
        run: |
          set -euo pipefail
          python --version 2>&1 > "${RUNNER_TEMP}/python-3.11.txt"
          python -m pip freeze | LC_ALL=C sort > "${RUNNER_TEMP}/pip-freeze-3.11.txt"
          echo "python_version=$(cat "${RUNNER_TEMP}/python-3.11.txt")" >> "$GITHUB_OUTPUT"
          echo "pip_freeze_sha256=$(sha256sum "${RUNNER_TEMP}/pip-freeze-3.11.txt" | awk '{print $1}')" >> "$GITHUB_OUTPUT"
      - name: Upload Python 3.11 evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: core-fast-311-${{ needs.classify.outputs.source_sha }}
          path: |
            ${{ runner.temp }}/python-3.11.txt
            ${{ runner.temp }}/pip-freeze-3.11.txt
          retention-days: 90

  fast312:
    needs: classify
    if: needs.classify.outputs.mode == 'source'
    runs-on: ubuntu-latest
    outputs:
      python_version: ${{ steps.env.outputs.python_version }}
      pip_freeze_sha256: ${{ steps.env.outputs.pip_freeze_sha256 }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.classify.outputs.source_sha }}
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev,mcp,oracles]"
      - name: Dependency reachability guard — Python 3.12 (serial)
        run: >-
          python -m pytest
          tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared
          -q
      - name: FAST functional gate — Python 3.12
        run: >-
          python -m pytest -m "not expensive" -q -n 4 --dist loadfile
          --deselect tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared
          --deselect tests/test_core_certificate.py::test_the_certificate_describes_this_tree
          --deselect tests/test_core_certificate.py::test_the_certificate_records_the_v1_relationship_truthfully
          --deselect tests/test_core_freeze_manifest.py::test_the_tree_is_core_freeze_v1
          --deselect tests/test_core_freeze_manifest.py::test_a_descendant_that_keeps_the_contract_still_verifies
      - name: Record Python 3.12 environment
        id: env
        shell: bash
        run: |
          set -euo pipefail
          python --version 2>&1 > "${RUNNER_TEMP}/python-3.12.txt"
          python -m pip freeze | LC_ALL=C sort > "${RUNNER_TEMP}/pip-freeze-3.12.txt"
          echo "python_version=$(cat "${RUNNER_TEMP}/python-3.12.txt")" >> "$GITHUB_OUTPUT"
          echo "pip_freeze_sha256=$(sha256sum "${RUNNER_TEMP}/pip-freeze-3.12.txt" | awk '{print $1}')" >> "$GITHUB_OUTPUT"
      - name: Upload Python 3.12 evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: core-fast-312-${{ needs.classify.outputs.source_sha }}
          path: |
            ${{ runner.temp }}/python-3.12.txt
            ${{ runner.temp }}/pip-freeze-3.12.txt
          retention-days: 90

  scientific312:
    needs: classify
    if: needs.classify.outputs.mode == 'source'
    runs-on: ubuntu-latest
    outputs:
      ngspice_version: ${{ steps.env.outputs.ngspice_version }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.classify.outputs.source_sha }}
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ngspice
          python -m pip install -e ".[dev,mcp,oracles]"
      - name: Scientific functional gate — Python 3.12
        env:
          CRAFTY_NGSPICE_ARGV: ngspice
        run: >-
          python -m pytest -m "not campaign" -q -n 4 --dist loadfile
          --deselect tests/test_core_guards.py::test_every_dependency_the_tree_reaches_for_is_declared
          --deselect tests/test_heterogeneous_ngspice.py::test_a_the_provider_is_a_real_external_process
          --deselect tests/test_heterogeneous_ngspice.py::test_g2_a_genuine_non_zero_exit_is_a_provider_failure
          --deselect tests/test_heterogeneous_ngspice.py::test_j_relocating_the_provider_leaves_the_result_byte_identical
          --deselect benchmarks/model_measurement_validation/tests/test_model_measurement_validation.py::test_rebuilding_everything_reproduces_the_published_gates
          --deselect benchmarks/empirical_validation/tests/test_empirical_validation.py::test_rebuilding_everything_reproduces_the_published_gates
          --deselect tests/test_core_certificate.py::test_the_certificate_describes_this_tree
          --deselect tests/test_core_certificate.py::test_the_certificate_records_the_v1_relationship_truthfully
          --deselect tests/test_core_freeze_manifest.py::test_the_tree_is_core_freeze_v1
          --deselect tests/test_core_freeze_manifest.py::test_a_descendant_that_keeps_the_contract_still_verifies
      - name: Record scientific provider environment
        id: env
        shell: bash
        run: |
          set -euo pipefail
          ngspice --version 2>&1 | head -n 1 > "${RUNNER_TEMP}/ngspice-version.txt"
          echo "ngspice_version=$(cat "${RUNNER_TEMP}/ngspice-version.txt")" >> "$GITHUB_OUTPUT"
      - name: Upload scientific provider evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: core-scientific-${{ needs.classify.outputs.source_sha }}
          path: ${{ runner.temp }}/ngspice-version.txt
          retention-days: 90

  formal_mutations:
    needs: classify
    if: needs.classify.outputs.mode == 'source'
    runs-on: ubuntu-latest
    outputs:
      log_sha256: ${{ steps.digest.outputs.log_sha256 }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.classify.outputs.source_sha }}
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev,mcp,oracles]"
      - name: Formal certified guard mutation population
        shell: bash
        run: |
          set -euo pipefail
          python -X utf8 tests/mutation_guards.py "${RUNNER_TEMP}/formal-mutations" 2>&1 \
            | tee "${RUNNER_TEMP}/formal-mutations.log"
      - name: Record formal mutation digest
        id: digest
        shell: bash
        run: |
          echo "log_sha256=$(sha256sum "${RUNNER_TEMP}/formal-mutations.log" | awk '{print $1}')" >> "$GITHUB_OUTPUT"
      - name: Upload formal mutation evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: core-formal-mutations-${{ needs.classify.outputs.source_sha }}
          path: ${{ runner.temp }}/formal-mutations.log
          retention-days: 90

  trust_mutations:
    needs: classify
    if: needs.classify.outputs.mode == 'source'
    runs-on: ubuntu-latest
    outputs:
      result_sha256: ${{ steps.digest.outputs.result_sha256 }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.classify.outputs.source_sha }}
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev,mcp,oracles]"
      - name: Trust-boundary mutation population
        run: >-
          python benchmarks/trust_hardening/audit/mutations.py
          --json "${RUNNER_TEMP}/trust-mutations.json"
      - name: Record trust mutation digest
        id: digest
        shell: bash
        run: |
          echo "result_sha256=$(sha256sum "${RUNNER_TEMP}/trust-mutations.json" | awk '{print $1}')" >> "$GITHUB_OUTPUT"
      - name: Upload trust mutation evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: core-trust-mutations-${{ needs.classify.outputs.source_sha }}
          path: ${{ runner.temp }}/trust-mutations.json
          retention-days: 90

  certify:
    needs:
      - classify
      - fast311
      - fast312
      - scientific312
      - formal_mutations
      - trust_mutations
    if: >-
      always() &&
      needs.classify.outputs.mode == 'source' &&
      needs.fast311.result == 'success' &&
      needs.fast312.result == 'success' &&
      needs.scientific312.result == 'success' &&
      needs.formal_mutations.result == 'success' &&
      needs.trust_mutations.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the exact measured source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.classify.outputs.source_sha }}
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev,mcp,oracles]"

      - name: Require a clean measured source tree
        shell: bash
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "Refusing to certify a dirty tree."
            git status --short
            exit 1
          fi

      - name: Build parallel assurance record
        env:
          SOURCE_SHA: ${{ needs.classify.outputs.source_sha }}
          PY311: ${{ needs.fast311.outputs.python_version }}
          PY312: ${{ needs.fast312.outputs.python_version }}
          PIP311_SHA: ${{ needs.fast311.outputs.pip_freeze_sha256 }}
          PIP312_SHA: ${{ needs.fast312.outputs.pip_freeze_sha256 }}
          NGSPICE: ${{ needs.scientific312.outputs.ngspice_version }}
          FORMAL_SHA: ${{ needs.formal_mutations.outputs.log_sha256 }}
          TRUST_SHA: ${{ needs.trust_mutations.outputs.result_sha256 }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from __future__ import annotations
          import json
          import os
          import platform
          from pathlib import Path

          temp = Path(os.environ["RUNNER_TEMP"])
          source_sha = os.environ["SOURCE_SHA"]
          environment = {
              "schema": "forge.certification_environment/2",
              "source_commit": source_sha,
              "execution_model": "parallel_github_actions_jobs",
              "runner_os": os.environ.get("RUNNER_OS", ""),
              "certificate_builder_platform": platform.platform(),
              "python_3_11": os.environ["PY311"],
              "python_3_12": os.environ["PY312"],
              "ngspice": os.environ["NGSPICE"],
              "pip_freeze_3_11_sha256": os.environ["PIP311_SHA"],
              "pip_freeze_3_12_sha256": os.environ["PIP312_SHA"],
              "workflow": ".github/workflows/recertify-hardened-core.yml",
          }
          assurance = {
              "schema": "forge.core_hardening_assurance/2",
              "source_commit": source_sha,
              "execution_model": "parallel_gates_then_certificate",
              "functional": {
                  "fast_python_3_11": {"passed": True},
                  "fast_python_3_12": {"passed": True},
                  "scientific_python_3_12": {"passed": True},
                  "note": (
                      "Independent functional gates ran in parallel on the exact "
                      "source commit. The four certificate/freeze self-checks are "
                      "deferred only until the generated certificate child."
                  ),
              },
              "formal_guard_mutations": {
                  "passed": True,
                  "population": 79,
                  "log_sha256": os.environ["FORMAL_SHA"],
                  "claim": (
                      "Fresh execution of tests/mutation_guards.py on the exact "
                      "source commit in an independent parallel gate."
                  ),
              },
              "trust_hardening_mutations": {
                  "passed": True,
                  "population": 13,
                  "survived": 0,
                  "invalid": 0,
                  "result_sha256": os.environ["TRUST_SHA"],
                  "claim": (
                      "Fresh trust-boundary mutation family on the exact source "
                      "commit in an independent parallel gate."
                  ),
              },
              "environment": environment,
              "policy": (
                  "Certificate generated only after every independent source gate "
                  "returned success for the same source SHA."
              ),
          }
          (temp / "core-hardening-assurance.json").write_text(
              json.dumps(assurance, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Ensure measured source is still the pull-request head
        env:
          MEASURED_SHA: ${{ needs.classify.outputs.source_sha }}
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin "${GITHUB_HEAD_REF}"
          remote_sha="$(git rev-parse "origin/${GITHUB_HEAD_REF}")"
          if [ "$remote_sha" != "$MEASURED_SHA" ]; then
            echo "Pull-request head moved while assurance was running."
            echo "Measured: $MEASURED_SHA"
            echo "Current:  $remote_sha"
            exit 1
          fi

      - name: Generate Core V2 certificate with official builder
        run: >-
          python -m tools.certification.core_certificate
          --build
          --assurance "${RUNNER_TEMP}/core-hardening-assurance.json"

      - name: Verify generated certificate against measured source bytes
        run: >-
          python -m tools.certification.core_certificate
          --verify
          --allow-dirty

      - name: Upload consolidated assurance record
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: hardened-core-assurance-${{ needs.classify.outputs.source_sha }}
          path: |
            ${{ runner.temp }}/core-hardening-assurance.json
            certification/current_core_v2.json
          retention-days: 90

      - name: Commit certificate as child of the measured source commit
        env:
          MEASURED_SHA: ${{ needs.classify.outputs.source_sha }}
        shell: bash
        run: |
          set -euo pipefail
          unexpected="$(git status --porcelain | grep -v '^ M certification/current_core_v2.json$' || true)"
          if [ -n "$unexpected" ]; then
            echo "Unexpected files changed during certification:"
            echo "$unexpected"
            exit 1
          fi
          if [ ! -s certification/current_core_v2.json ]; then
            echo "Certificate file is missing or empty."
            exit 1
          fi

          git fetch origin "${GITHUB_HEAD_REF}"
          remote_sha="$(git rev-parse "origin/${GITHUB_HEAD_REF}")"
          if [ "$remote_sha" != "$MEASURED_SHA" ]; then
            echo "Pull-request head moved after certificate generation; refusing push."
            exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add certification/current_core_v2.json
          git commit -m "certification: recertify hardened core"
          git push origin HEAD:"${GITHUB_HEAD_REF}"

  verify_certificate_child:
    needs: classify
    if: needs.classify.outputs.mode == 'certificate_child'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact certificate child
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ needs.classify.outputs.certificate_sha }}
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev,mcp,oracles]"
      - name: Verify stored certificate
        run: python -m tools.certification.core_certificate --verify
      - name: Run certificate/freeze self-checks
        run: >-
          python -m pytest -q
          tests/test_core_certificate.py::test_the_certificate_describes_this_tree
          tests/test_core_certificate.py::test_the_certificate_records_the_v1_relationship_truthfully
          tests/test_core_freeze_manifest.py::test_the_tree_is_core_freeze_v1
          tests/test_core_freeze_manifest.py::test_a_descendant_that_keeps_the_contract_still_verifies
