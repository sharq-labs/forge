{
 "schema": "core_v4_false_confidence_threshold_protocol/1",
 "batch": 54,
 "improvements": [
  "I-25 part C, R-43's producer half (the reach ledger's row for R-43 names I-25 as what closes it)"
 ],
 "problems_addressed": [
  "R-43 (the audit's findings 51 and 86), the producer half"
 ],
 "audit": "benchmarks/core_v4_false_confidence/REAUDIT_2026-09-16.json",
 "baseline_commit": "c162d9da",
 "written_before_any_rule_below_was_implemented_or_scored": true,
 "what_this_part_is": "R-43 is the last LIBRARY_ONLY row in the guard reach ledger. Batch 8 (I-09/I-10) gave the credibility report an `uncertainty` field carrying `source_kind`, declared PARAMETER and COMBINED on the two V1 predictive intervals, and stated the map `CHANNEL_ACCEPTS_SOURCE` that refuses a record filed under a channel its own source contradicts. Its own status line says what stayed open, in three sentences: every domain solver still emits UNSPECIFIED so the map has something true to check only for those two intervals; UNSPECIFIED is NAMED through `unattributed_channels` rather than refused; and nothing carries a V1 interval into an `UncertaintyDeclaration`, so the producer and the budget are two unconnected halves.",
 "reach": "The PRODUCER half is REACHED: `posterior_predictive_uq` is the V1 path the production calibration study predicts through (the audit's finding 84 says so in its own words), and it is where the two declared intervals are built. The BUDGET half is library-only: nothing in `src` builds an `UncertaintyDeclaration`, so `budget_from_declaration` has no production caller, and this batch does not invent one -- what it does is make the two halves connectable and refuse the shape that made the gap invisible.",
 "what_was_already_seen": {
  "finding 51 (R-43)": "`UncertaintySource` has no producer, so `source_kind` is UNSPECIFIED on every uncertainty production makes, and an UNSPECIFIED kind is indistinguishable from a kind nobody was asked for. The SRIA budget then marks aleatoric and model_form 'known' from a numerical-only record.",
  "finding 86 (R-43)": "The credibility report had no uncertainty field at all, so the record could not reach a verdict even where it existed. Closed in batch 8; what this batch is about is the producer and the budget it was supposed to feed."
 },
 "rules": {
  "an_unattributed_quantified_record_is_not_aggregated_into_a_total": {
   "improvement": "I-25",
   "problem": "R-43",
   "rule": "`UncertaintyBudget.aggregate` refuses to combine a quantified channel record whose own `source_kind` is UNSPECIFIED. The record may still be declared, read and named by `UncertaintyDeclaration.unattributed_channels`.",
   "class": "A",
   "why": "The audit's finding 51 measures the aggregation: 'The SRIA budget marks aleatoric and model_form known from a numerical-only record' -- and a marked state becomes a NUMBER only through aggregate, which root-sum-squares channels into a total attributed to each of them. A record nobody attributed is a number nobody has said is that channel's, and combining it is where the claim is made. Refusing at construction or at declaration instead would break the SHA-256-pinned E1 and E2 harnesses, which is amendments 1 and 2.",
   "closes": "finding 51's budget claim, at the point the claim is made.",
   "amended": "This replaces the preregistered rules 1 and 2; see the amendment log for the pinned bytes that forced it."
  },
  "a_predictive_interval_can_be_carried_into_a_declaration": {
   "improvement": "I-25",
   "problem": "R-43",
   "rule": "A new public function in `engcore.sria.uncertainty`: `channels_from_predictive_uncertainty(records)`, which maps each quantified V1 uncertainty onto the channel its own `source_kind` names, refuses COMBINED with the reason the map already gives, and skips UNKNOWN. The two halves are then connected by a call rather than by a reader's assumption. An unattributed quantified record is REFUSED rather than skipped, because skipping would drop a number silently.",
   "class": "A",
   "why": "The third open sentence of batch 8's status, stated as code: the producer declares PARAMETER and the budget accepts PARAMETER only under EPISTEMIC_PARAMETER, and nothing carried the one to the other, so the map had nothing production-made to check. Deriving the channel from the record's own declaration is the only direction that cannot invent an attribution.",
   "closes": "the 'two unconnected halves' residual, to the extent a library can: the caller that builds a declaration is still SRIA's, and this is the function it now has."
  }
 },
 "thresholds": {
  "none_introduced": {
   "value": null,
   "class": "A",
   "why": "every rule is a structural requirement about what a record declares. No number is introduced, and no existing threshold moves."
  }
 },
 "compatibility": {
  "additive_only": true,
  "what_is_added": [
   "channels_from_predictive_uncertainty in engcore/sria/uncertainty.py, module-public and NOT added to any package export list (batch 45's amendment 1: the frozen V1 symbol count stays 194 until I-30)"
  ],
  "what_is_not_changed": [
   "no field, default, enum member or signature is removed, renamed or reordered",
   "UncertaintyKind keeps its three members and Uncertainty keeps its fields; the serialized shape of every existing record is unchanged",
   "UNKNOWN records -- which is what every domain solver in this repository emits -- are untouched on both rules"
  ],
  "records_that_already_exist": "A committed record carrying a QUANTIFIED uncertainty with no declared source becomes unreadable, because `from_dict` builds through the constructor. Every committed JSON is searched for one in this batch and the result is named in the audit document; the two V1 intervals production writes both declare their source as of batch 8, so a record written by this tree since then already satisfies the rule.",
  "deliberate_narrowing": "A quantified uncertainty that does not say what it is an uncertainty of stops being constructible, and an UNSPECIFIED quantified record stops being filable under any channel. In the tree this falls on TEST fixtures only -- 42 construction sites across 14 files -- and each is amended in place with the reason."
 },
 "residuals_stated_in_advance": [
  "No domain solver in this repository quantifies uncertainty at all: every one emits `Uncertainty.unknown` with a stated reason, which is the honest state of a solve nobody put an uncertainty budget on. This batch does not invent one -- fabricating a number for a channel is the defect the whole round is about -- so the producer that exists is the V1 predictive path, and the domains' silence stays visible as UNKNOWN.",
  "`budget_from_declaration` still has no production caller: nothing in `src` builds an `UncertaintyDeclaration`. The ledger row keeps saying so, and the new function is what a caller would use.",
  "The refusal is on KIND, not on truth: a caller may declare MEASUREMENT on a discretization estimate, and no rule here can tell. What is closed is silence.",
  "`unattributed_channels` stays on the declaration record. With the rules above it can only report an UNKNOWN channel, which is what it then means, and it is left in place because a reader asking 'which channels said nothing' is asking a legitimate question."
 ],
 "verification_plan": [
  "the batch's own test file, with the xfail markers removed",
  "tests/test_uncertainty_contract.py and the SRIA suites, the uq suites, and every test file that builds a quantified Uncertainty",
  "a search of every committed JSON for a quantified uncertainty record with no declared source",
  "FAST tier: python -m pytest -m \"not expensive\" -q -n 4 --dist loadfile, at the by-design set (19)",
  "expensive tier at the recorded baseline (528 passed, 18 failed, 14 errors)",
  "python -m pytest tests/test_mutation_harness.py, with tests/mutation_guards.py untouched",
  "the batch's guard mutations, isolated, with an unmutated control, re-running the pinned mutations that target the changed files",
  "python -m tools.certification.guard_reach --verify and python -m tools.certification.assert_clean_tree"
 ],
 "amendment_log": [
  {
   "date": "2026-09-18",
   "amendment": "Rule 1 is WITHDRAWN: `Uncertainty` does not refuse a quantified record whose source_kind is UNSPECIFIED.",
   "numbers_that_forced_it": "Implemented as preregistered, the constructor refusal failed 218 FAST-tier tests. Most were fixtures and were amended, but two of the files that build such records are SHA-256 PINNED: experiments/electrical_e1/e1_harness.py and experiments/electrical_e2/e2_harness.py each construct quantified channel records with no declared source, and tests/test_electrical_v01_demo.py::test_3_frozen_experiment_artifacts_are_unchanged and tests/test_sria_e2_model_adequacy.py::test_2_e1_frozen_files_are_unchanged compare their bytes with a frozen digest ('d9bcb71b11c94b8f755a6213b41dac7fc38b4d3903142b0d53495c0108112626' for e1_harness.py). Editing them is forbidden and 54 tests in tests/test_sria_e2_model_adequacy.py fail without the edit, so a constructor refusal makes a pinned experiment unrunnable rather than making anything more honest. The rule moves to where the audit measured the harm; see amendment 2.",
   "threshold_moved": "none: no threshold is introduced by this batch."
  },
  {
   "date": "2026-09-18",
   "amendment": "Rules 1 and 2 are replaced by one rule at the place the number is USED: `UncertaintyBudget.aggregate` refuses to combine a quantified channel record whose own source_kind is UNSPECIFIED, and `channels_from_predictive_uncertainty` refuses to carry one. `CHANNEL_ACCEPTS_SOURCE` keeps UNSPECIFIED in every row, with the reason written beside it.",
   "numbers_that_forced_it": "The same pinned bytes as amendment 1: the E1 harness FILES an unattributed quantified record under the ALEATORIC channel, so a refusal at declaration time -- rule 2 as preregistered -- breaks it too (tests/test_sria_e2_model_adequacy.py, 54 failures). What the audit's finding 51 actually measures is the aggregation: 'The SRIA budget marks aleatoric and model_form known from a numerical-only record', and a marked state becomes a number only through `aggregate`. So an unattributed record may be declared and read -- and `unattributed_channels` still names it, which is what makes the refusal readable -- and it may not be combined into a total attributed to channels nobody attributed. Four in-tree SRIA tests that aggregate now declare the source of each channel they file, each amended in place with the reason; the FAST tier is back at the by-design 19.",
   "threshold_moved": "none."
  }
 ]
}
