"""Scientific credibility and evidence-translation layer.

This package owns V&V report semantics and translation into SRIA evidence.
It deliberately sits outside engcore.mcp so scientific reasoning never
depends on a transport adapter. Legacy MCP import paths are compatibility
shims only.
"""
