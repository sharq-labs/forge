"""Core V2: routed uncertainty quantification -- a resolved grid, or a named local-Gaussian route, or a refusal.

The public surface is defined in docs/CORE_V2_API_DESIGN.md before it is implemented. This package adds
to Core V1 and changes nothing in it: no V1 canonical module exports a V2 name, so the V1 frozen digest
cannot move.
"""

__all__: list[str] = []
