# Applicability conditions

| Model | Condition | Quantity | Bound | Physical justification | Source |
|---|---|---|---|---|---|
| `thermal.lumped.first_order_capacity` | `biot_number` | Bi = h·L_c/k, with L_c declared or V/A_s and h = hA/A_s | ≤ 0.1 (`LUMPED_BIOT_LIMIT`) | Bi is the temperature drop inside the body over the drop across its surface film, and this model asserts the internal drop is zero. At Bi ≤ 0.1 the internal drop stays within about 10 % of the surface drop, so a single body temperature is defensible. | Incropera, DeWitt, Bergman & Lavine, *Fundamentals of Heat and Mass Transfer*, 6th ed. (Wiley, 2007), §5.1, Eq. 5.10 |
| `thermal.lumped.first_order_capacity` | `internal_fourier_number` | Fo = (t/τ)/Bi, τ = C/hA | ≥ 0.2 (`LUMPED_MIN_FOURIER_NUMBER`) | The horizon is long enough that the exact series solution is within ≈2 % of its first term alone, and a first-term-only response is the single exponential a one-capacity model computes. A conservative screen: below 0.2 the lumped model is not shown to be wrong, only unvalidated by this criterion — at small Bi the higher modes carry O(Bi) amplitude and may be negligible sooner. | Incropera et al., 6th ed. (2007), §5.5.2 (one-term approximation, ≈2 % for Fo > 0.2), with the identity Bi·Fo = t/τ from §5.2, Eq. 5.12 |
| `thermal.lumped.first_order_capacity` | `conductance_excursion_ratio` | max\|T − T_amb\| ÷ declared constant-hA span; 0 under a declared forced regime | ≤ 1 (`EXCURSION_BUDGET_LIMIT`) | Under free convection the surface coefficient follows the driving difference, h ~ ΔT^(1/4) for a laminar external layer, so a constant hA holds only over a stated span. Under forced convection the flow rather than the buoyancy sets h, and the ΔT dependence this condition bounds is absent. | Incropera et al., 6th ed. (2007), §9.2, Eq. 9.19 ff. (free-convection Nu ~ Ra^(1/4)) |
| `thermal.lumped.first_order_capacity` | `capacity_excursion_ratio` | \|T_ss − T_0\| ÷ declared constant-C span | ≤ 1 (`EXCURSION_BUDGET_LIMIT`) | C = ρVc_p and the specific heat of a real solid rises measurably over engineering temperature ranges. How much drift a study tolerates is its own choice, so the span is declared per material rather than fixed here. | Incropera et al., 6th ed. (2007), Table A.1, which tabulates c_p at several temperatures for each solid |
| `thermal.lumped.first_order_capacity` | `radiation_to_convection_ratio` | h_r/h, h_r = εσ(T_s+T_sur)(T_s²+T_sur²) at the peak surface temperature | ≤ 0.1 (`RADIATION_NEGLIGIBILITY_LIMIT`) | Radiation and convection act in parallel over the same surface, so their coefficients add and this ratio is the fractional error of the model's declared "no radiation" assumption. The 0.1 ceiling is the same neglected-mechanism convention the text applies to the lumped criterion, and is recorded as a convention. | Incropera et al., 6th ed. (2007), §1.2.3, Eq. 1.9 (h_r); σ = 5.670374419×10⁻⁸ W m⁻² K⁻⁴, BIPM, *The International System of Units (SI)*, 9th ed. (2019), §2.3.1 |
| `thermal.lumped.first_order_capacity` | `melting_temperature_utilization` | max(T_0, T_ss) ÷ declared phase-change temperature | ≤ 1 (`PHASE_CHANGE_UTILIZATION_LIMIT`) | The balance carries no latent-heat term and no second phase, so above the phase change it is not inaccurate but describing a body that is no longer there. The trajectory is monotone between its endpoints, so the endpoint maximum bounds it exactly. | Incropera et al., 6th ed. (2007), §5.1 (lumped formulation for a single-phase solid) |
| `electrical.material.rated_linear_tcr_resistance` | `linearization_excursion_ratio` | \|T − T_ref\| ÷ material-declared linearization band | ≤ 1 (`LINEARIZATION_BUDGET_LIMIT`) | R(T) = R_ref(1 + α(T − T_ref)) is the first-order Taylor expansion of ρ(T) about T_ref, and the term it drops grows as (T − T_ref)². The coefficient of that term belongs to the material's own ρ(T) curve, so the band is declared per material and never fixed here. | Ashcroft & Mermin, *Solid State Physics* (Holt-Saunders, 1976), Ch. 26 (temperature dependence of the ideal resistivity) |
| `electrical.material.rated_linear_tcr_resistance` | `operating_temperature_utilization` | T ÷ material-declared maximum operating temperature (both absolute) | ≤ 1 (`OPERATING_TEMPERATURE_LIMIT`) | A hard material limit rather than a modelling tolerance: above its rating a conductor is annealing, oxidising or open. No temperature coefficient describes a part that is no longer the part that was specified. | IEC 60115-1, *Fixed resistors for use in electronic equipment — Part 1: Generic specification*, Clause 2 (category temperature and rated dissipation), and the derating characteristic the same standard requires above the rated ambient |
| `electrical.material.rated_linear_tcr_resistance` | `reduced_debye_temperature` | T ÷ material-declared Debye temperature θ_D | ≥ 1/3 (`BLOCH_GRUENEISEN_LINEAR_FLOOR`) | Above roughly this fraction of θ_D the phonon population scattering the conduction electrons is classical and a metal's ideal resistivity is linear in T; well below it Bloch–Grüneisen gives ρ ~ T⁵ and no single coefficient fits. The 1/3 is the conventional engineering reading of "T ≫ θ_D" and is recorded as a convention, not as a printed threshold. | Ashcroft & Mermin, *Solid State Physics* (1976), Ch. 26, Eq. 26.55; Kittel, *Introduction to Solid State Physics*, 8th ed. (Wiley, 2005), Ch. 6 |
| `electrical.material.rated_linear_tcr_resistance` | `linear_resistance_ratio` | 1 + α(T − T_ref) = R(T)/R_ref | > 0, strictly (`MINIMUM_LINEAR_RESISTANCE_RATIO`) | Every straight line with a non-zero slope crosses zero, and past the crossing the expression describes a negative conductor rather than a poor one. The bound is the physics of the computed quantity, so it needs no tolerance and no citation. | The model's own constitutive form; cf. Incropera-style admissibility, and the solver's post-hoc `resistance_strictly_positive` check, which this condition anticipates |
| `electrical.dc.resistor_ohm` | `dissipated_power_utilization` | V·I ÷ (derating × declared rated dissipation) when the rating is a bare wattage; **(T_amb + (V·I ÷ derating)·R_implied) ÷ T_zero** when `rated_power_temperature` and `zero_power_temperature` are also declared, with R_implied = (T_zero − T_rated) ÷ rated dissipation | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | Above its rated dissipation an element's temperature rise carries it out of the tolerance band it was specified in, and the constant, temperature-independent resistance this model assumes is the first thing lost. **A rated dissipation is a pair, not a number** — 0.4 W *at 70 °C*, falling along a derating characteristic to zero at the permissible film temperature — and until the two temperatures could be declared, a part in a 120 °C ambient was compared against a rating it no longer had. The second form is that characteristic. It is written on the temperature axis rather than as V·I ÷ P_effective because the power form is infinite at and above T_zero, where a real part still has a real answer: it may not be used at all. The two forms are algebraically the same statement wherever both are defined and cross the bound together. The reciprocal slope of the line, (T_zero − T_rated) ÷ P_rated, is a thermal resistance — a derating curve is the manufacturer stating one as a line instead of a number. **Two readings under one name, like `convection_property_range_utilization` below**: which applies is decided by what the caller declared, never by a default. Declaring neither temperature leaves the first form exactly as it was; declaring one without the other is refused; declaring both without an ambient is UNKNOWN rather than answered from the printed number. Rated dissipation is defined against a stated reference ambient, so the derating fraction is a declared input rather than a constant. | IEC 60115-1, Clause 2 (rated dissipation, rated ambient temperature, derating curve) |
| `electrical.dc.resistor_ohm` | `working_voltage_utilization` | \|V\| ÷ (derating × declared maximum working voltage) | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | A second and independent limit: for a high-value element the voltage rating binds long before the dissipation rating does. Exceeding it breaks the element down across its body or coating rather than by dissipation. | IEC 60115-1, Clause 2 (maximum element voltage, specified alongside rated dissipation) |
| `electrical.dc.ideal_voltage_source` | `source_current_utilization` | \|I\| ÷ (derating × declared maximum current) | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | The model's declared assumption is unlimited current compliance, which no real source has. Past its current limit a supply folds back or drops out of regulation, and the terminal voltage stops being the imposed constant this model asserts. | The model's own declared assumption "unlimited current compliance"; limit value supplied per component |
| `electrical.dc.ideal_current_source` | `compliance_voltage_utilization` | \|V_terminal\| ÷ (derating × declared compliance voltage) | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | The dual of the voltage source's limit and the direct falsification of this model's declared unlimited compliance voltage. Above its compliance range the delivered current is set by the network, not by the source. | The model's own declared assumption "unlimited compliance voltage"; limit value supplied per component |
| `battery.cell.rint_ocv` | `continuous_c_rate_utilization` | \|I/Q_nom\| ÷ declared continuous discharge C-rate | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | The most common way a cell sizing is wrong: a current the arithmetic delivers and the cell does not. Above its continuous rating a cell is outside the conditions its resistance, capacity and temperature limits were all specified under. The rating belongs to the cell and is declared per cell; **the bound is definitional**, being the fraction of that rating in use. | Per-cell datasheet rating supplied by the caller; C-rate as a multiple of nominal capacity per Plett, G. L., *Battery Management Systems, Volume I: Battery Modeling*, Artech House (2015), Ch. 2 |
| `battery.cell.rint_ocv` | `pulse_c_rate_utilization` | \|I_peak/Q_nom\| ÷ declared pulse discharge C-rate | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | A second, independent limit: a cell rated 1C continuous is commonly rated several C for a short pulse, because the continuous constraint is thermal and accumulates while the pulse constraint does not. UNKNOWN unless the duty declares a pulse current *and* the cell declares a pulse rating. **Bound definitional.** | Per-cell datasheet pulse rating supplied by the caller |
| `battery.cell.rint_ocv` | `pulse_duration_utilization` | declared pulse length ÷ length the pulse rating is published for | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | A peak rating is meaningless without its duration: what it bounds is the heat deposited and the overpotential reached during the pulse, and both grow with its length, so a cell's 10 s and 30 s pulse ratings differ. Separate from the peak-current condition because the two fail independently. **Bound definitional.** | Per-cell datasheet rated pulse duration supplied by the caller |
| `battery.cell.rint_ocv` | `soc_window_margin` | min(z_min − w_lo, w_hi − z_max) ÷ (w_hi − w_lo) | ≥ 0 (`WINDOW_MARGIN_FLOOR`) | Both of this model's shortcuts fail at the ends of the charge axis: the affine OCV is a chord across a curve with a knee near empty and a plateau near full, and a constant R_int is least defensible near empty where charge-transfer and diffusion resistance rise. The window is the caller's statement of where they hold. Monotone under constant current, so the endpoints bound the trajectory exactly. **Bound definitional** — 0 is the declared edge. | Plett, *Battery Management Systems, Vol. I* (2015), Ch. 3, which treats OCV as tabulated data precisely because no two-point line reproduces it everywhere |
| `battery.cell.rint_ocv` | `discharge_temperature_position` | (T − T_min) ÷ (T_max − T_min), declared discharge range | ∈ [0, 1] (`TEMPERATURE_POSITION_FLOOR`, `TEMPERATURE_POSITION_CEILING`) | The cell sits inside the discharge temperature range declared for it. **DISCHARGE ONLY**: a cell's charge range is narrower, most sharply at the cold end where charging plates lithium rather than intercalating it. This domain models discharge, and no condition here would catch a caller charging the cell. **Both bounds definitional**, the position being constructed so 0 and 1 are the declared edges. | Per-cell datasheet discharge temperature range supplied by the caller |
| `battery.cell.rint_ocv` | `internal_resistance_drift_ratio` | \|T − T_R,ref\| ÷ declared span over which one R_int carries | ≤ 1 (`DECLARED_BUDGET_LIMIT`) | A cell's internal resistance is dominated by electrolyte conductivity and charge-transfer kinetics, both thermally activated, and can more than double between room temperature and freezing. A single declared R_int is a claim about one temperature. No functional form is fitted here: the reference and the span are the cell's to declare, and **the bound of 1 is the fraction of the caller's own declared span consumed**. | Plett, *Battery Management Systems, Vol. I* (2015), Ch. 3 (measuring equivalent-circuit resistances and their dependence on operating conditions) |
| `battery.cell.rint_ocv` | `self_heating_rise_ratio` | (I²·R_int ÷ hA) ÷ declared isothermal-cell budget | ≤ 1 (`DECLARED_BUDGET_LIMIT`) | Every model in this domain assigns the cell **one** temperature; a cell tens of kelvin above its ambient is not at one temperature, and the R_int and capacity that matter are those at its core. The asymptote rather than the end-of-interval rise, because a short horizon defers an excursion rather than avoiding it. **Bound definitional.** | Incropera, DeWitt, Bergman & Lavine, *Fundamentals of Heat and Mass Transfer*, 6th ed. (Wiley, 2007), §5.3, Eq. 5.25 (lumped body with internal generation, T_ss − T_amb = Q/hA) |
| `battery.cell.rint_ocv` | `polarization_unmodelled_fraction` | min(f, 1 − f) for f = 1 − exp(−t/τ_pol), the share of the diffusion overpotential still in motion at the end of the interval | ≤ 0.05 (`POLARIZATION_UNMODELLED_CEILING`) — **a convention, not a cited threshold** | The Rint circuit represents the whole overpotential as an instantaneous ohmic drop, while a cell's diffusion overpotential grows as 1 − exp(−t/τ) after a current step. **Two regimes admit a constant resistance and the band between them does not.** *Settled* (f ≥ 0.95, t ≥ 3.0 τ_pol): the branch has stopped moving and contributes a constant I·R_diff that an R_int measured over a comparable interval already contains. *Undeveloped* (f ≤ 0.05, t ≤ 0.051 τ_pol): the branch has barely begun and the terminal voltage is essentially the instantaneous ohmic drop a short-pulse R_int represents. Around t ~ τ_pol the branch slews through the interval and no constant resistance reproduces the terminal voltage — that band, and only that band, is excluded. **0.05 is one convention read from both ends** (the same reading that makes three time constants mean settled, exp(−3) = 0.05); **no source below prints it as a threshold for this quantity, and both resulting bounds are recorded as conventions rather than dressed as citations.** A conservative screen: inside the band the model is not shown to be wrong, only unvalidated by this reading. It does not check that R_int was characterised in the regime it is used in, which this domain cannot declare. | Physics cited, number not: Plett, *Battery Management Systems, Volume I: Battery Modeling*, Artech House (2015), Ch. 3, which adds a series RC branch to the Rint model for exactly this reason and prints no threshold |
| `battery.cell.rint_ocv` | `terminal_voltage_ratio` | V/OCV = 1 − I·R_int/OCV at the end of the interval | > 0, strictly (`MINIMUM_TERMINAL_VOLTAGE_RATIO`) | Every straight line with a non-zero slope crosses zero; past the crossing the expression describes not a deeply loaded cell but one sourcing current at a negative terminal voltage. **The bound is the physics of the computed quantity, not a tolerance**, which is why it is exactly zero and needs no source. A consistency condition rather than an applicability one, and not counted among the fourteen. | The model's own constitutive form; the direct analogue of `electrical.material.rated_linear_tcr_resistance`'s `linear_resistance_ratio` |
| `battery.cell.coulomb_counting` | `soc_step_resolution_ratio` | \|z_0 − z_end\| ÷ declared state-of-charge step resolution | ≤ 1 (`STEP_RESOLUTION_LIMIT`) | The integration is exact for a constant current, so this bounds not the integral but everything the step holds fixed while z moves: OCV and terminal voltage are evaluated once per step, at one state of charge, and both are functions of it. A load that varies within the step must be resolved by steps short enough to sample it — the same requirement seen from the load's side. How much OCV movement is tolerable depends on that cell's curve, so the resolution is declared and **the bound is definitional**. | Plett, *Battery Management Systems, Vol. I* (2015), Ch. 2 (the coulomb-counting integral) and Ch. 3 (the OCV relationship) |
| `battery.cell.coulomb_counting` | `capacity_temperature_drift_ratio` | \|T − T_Q,ref\| ÷ declared span over which one capacity carries | ≤ 1 (`DECLARED_BUDGET_LIMIT`) | The counter divides by Q_nom, and Q_nom is a measurement under stated conditions. Normalizing by a rating taken somewhere else reports a state of charge against a capacity the cell does not have. Distinct from the resistance drift condition — different property, different reference, different span — and the two must be able to disagree. **Bound definitional.** | IEC 61960-3:2017, *Secondary cells and batteries containing alkaline or other non-acid electrolytes — Secondary lithium cells and batteries for portable applications*, Clause 7 (discharge performance; the ambient temperature rated capacity is measured at, with separate low- and high-temperature performance) |
| `battery.cell.coulomb_counting` | `soc_window_margin` | as above | ≥ 0 (`WINDOW_MARGIN_FLOOR`) | Shared derived quantity, load-bearing here for this model's own reason: outside the declared window the nominal capacity is not the charge the cell will actually give up, so the counter's denominator stops meaning what it divides. This is also where a counter driven below empty or above full is caught. **Bound definitional.** | Plett, *Battery Management Systems, Vol. I* (2015), Ch. 2 (state of charge bounded by its own definition) |
| `battery.cell.constant_current_runtime` | `cutoff_consistency_margin` | declared z_cutoff − state of charge at which V reaches the declared V_cutoff | ≥ 0 (`CUTOFF_CONSISTENCY_FLOOR`) | At high current I·R_int is subtracted from every point of the OCV curve, so the voltage cutoff bites at a **higher** state of charge and the requested depth of discharge is never reached — a runtime computed to it is over-reported, silently, by whatever fraction of the capacity lies between the two. The two cutoffs are independent declarations and this is the only place their consistency is checked. **Bound definitional**: the two meeting exactly is consistent, not a violation. | Chord inversion of the model's own Rint relation; Plett, *Battery Management Systems, Vol. I* (2015), Ch. 3 |
| `battery.cell.constant_current_runtime` | `continuous_c_rate_utilization` | as above | ≤ 1 (`RATING_UTILIZATION_LIMIT`) | Shared derived quantity, load-bearing here for this model's own reason: a runtime is by construction a claim about a current held for a long time, which is exactly what a continuous rating bounds. **Bound definitional.** | Per-cell datasheet rating supplied by the caller |
| `battery.cell.constant_current_runtime` | `soc_window_margin` | as above | ≥ 0 (`WINDOW_MARGIN_FLOOR`) | The trajectory over the declared interval stays where the caller declared the OCV chord and the constant R_int the runtime rests on actually hold. **Bound definitional.** | Plett, *Battery Management Systems, Vol. I* (2015), Ch. 3 |
| `battery.cell.peukert_capacity_derating` | `peukert_extrapolation_ratio` | \|log₁₀(I/I_ref)\| ÷ declared decades the fit covers | ≤ 1 (`DECLARED_BUDGET_LIMIT`) | The extracted exponent is not a constant of the cell: it varies with the discharge current it is measured over. The law is therefore an interpolation across the currents it was fitted on, and using it far outside them extrapolates a fit whose own parameter has moved. Decades rather than a ratio because the law is linear in log I. **Bound definitional**, the reach being the caller's declaration. | Doerffel, D. & Sharkh, S. A., "A critical review of using the Peukert equation for determining the remaining capacity of lead-acid and lithium-ion batteries", *Journal of Power Sources* 155 (2006), 395–400 |
| `battery.cell.peukert_capacity_derating` | `peukert_capacity_ratio` | Q_eff/Q_nom, with Q_eff = Q_nom·(I_ref/I)^(k−1) | ≤ 1 (`PEUKERT_DERATING_LIMIT`) | Peukert's law describes capacity **lost** to rate. Below the reference current the same expression predicts a cell delivering more than the capacity it is rated for, which is not a conservative error but the formula read outside the direction it means anything in. **Definitional and directional, not a tolerance.** | Peukert, W., "Ueber die Abhaengigkeit der Kapacitaet von der Entladestromstaerke bei Bleiakkumulatoren", *Elektrotechnische Zeitschrift* 20 (1897), 287–288 |
| `battery.cell.peukert_capacity_derating` | `peukert_temperature_drift_ratio` | \|T − T_k,ref\| ÷ declared span over which one exponent carries | ≤ 1 (`DECLARED_BUDGET_LIMIT`) | Temperature stands alongside current as a variable the extracted exponent depends on, which is the substance of the source's objection to using the equation as a general remaining-capacity model. Distinct from the capacity and resistance drift conditions: a different fitted object, with its own reference and its own declared reach. **Bound definitional.** | Doerffel, D. & Sharkh, S. A., *Journal of Power Sources* 155 (2006), 395–400 |
| `kinetics.cstr.nonisothermal_first_order` | `damkohler_number` | Da = k(T_f)·τ, k(T) = k0·exp(−E/RT), τ = V/q | ≤ 10 (`MAX_WELL_MIXED_DAMKOHLER`) — **a convention, not a cited threshold** | The model's first assumption is a perfectly mixed tank. A tank homogenizes in a blend time t_m, and the idealization needs it to do so at least as fast as the reaction changes the local state, t_m ≤ 1/k; with Da = kτ that is Da ≤ τ/t_m, so the ceiling is set by the tank's own mixing quality. Past it the fluid reacts in partially segregated parcels rather than at one state. **The 10 is the conventional well-stirred reading τ/t_m = 10; no source prints it as a threshold for this quantity and it is recorded as a convention.** Bounded from **above only**: at Da → 0 the tank is a mixing vessel and the equations still describe it exactly, and Da ~ 1 is where multiplicity and ignition live, which is what this model is for. **This bounds the temperature field, not the concentration field** — a first-order rate is linear in C_A and the degree of segregation does not change its conversion, but k(T) is convex in T, so any spread in temperature makes the mean rate exceed the rate at the mean. | Physics cited, number not: Levenspiel, O., *Chemical Reaction Engineering*, 3rd ed. (Wiley, 1999), Ch. 16 ("Earliness of Mixing, Segregation and RTD"), which also gives the first-order invariance the caveat rests on |
| `kinetics.cstr.nonisothermal_first_order` | `adiabatic_ceiling_temperature` | max(T_0, T_f, T_c) + β·max(C_A0, C_Af), β = (−ΔH)/(ρc_p) | ≤ 1000 K (`MAX_VALID_TEMPERATURE_K`) | The same single-phase constant-property envelope the `temperature` condition uses, asked at the hottest state the declaration can reach rather than at the one it starts from, so it **introduces no new threshold**. An upper bound rather than an estimate: Z = T + βC_A is this reactor's exact invariant, dZ/dt = a(Z_f − Z) − γ(T − T_c), and with C_A ≥ 0 and C_A ≤ max(C_A0, C_Af) it follows that T ≤ Z ≤ max(T_0, T_f, T_c) + β·max(C_A0, C_Af) with or without cooling. A declaration whose ceiling clears the envelope cannot be repaired by a shorter horizon or a tighter tolerance. Conservative by construction: a strongly cooled reactor is still reported as *able* to reach it, because this condition does not certify that the cooling holds. β ≤ 0 clamps the rise to zero — an endothermic reaction cannot lower its own ceiling below a temperature the tank is fed at. | The model's own exact invariant, implemented in `kinetics/cstr/reference.py` and stated in the `problem.py` docstring; the envelope from Aris, R. & Amundson, N. R., *Chemical Engineering Science* 7(3) (1958), 121–155 as parameterized by Seborg, Edgar, Mellichamp & Doyle, *Process Dynamics and Control* |
| `thermal.lumped.first_order_capacity` | `convection_flow_range_utilization` | Ra_L/10⁹ under free convection, Re_L/5×10⁵ under forced; Ra_L = gβΔT·L³/(να), Re_L = u·L/ν, with L the **convection length** and ΔT the largest surface-to-ambient difference | ≤ 1 (`CORRELATION_RANGE_LIMIT`) | The declared `ambient_conductance` had no stated basis, and every other condition here is computed **from** it — an hA wrong by 2× moves Bi, both excursion budgets and the radiation share by 2× and none of them notices. This asks whether the correlation the caller took h from is being read inside the range its source states. **Neither underlying number is a convention**: 10⁹ is the vertical free-convection transition Churchill & Chu attach to their laminar equation, and 5×10⁵ is the flat-plate transition Reynolds number Incropera prints with Eq. 7.30. The bound of 1 is definitional, being the fraction of that range in use, and folding both routes into one utilization is what keeps a free-convection body (no velocity) and a duct (no buoyancy term) from being permanently UNKNOWN on a condition that cannot apply to it. **Geometry and orientation are part of each correlation and nothing declared carries them** — a horizontal plate is 0.54/0.27·Ra^(1/4) over a different length (§9.6.3), a cylinder in cross-flow is Hilpert or Zukauskas (§7.4), a duct is Dittus–Boelter over a hydraulic diameter (§8.5) — so this domain cannot detect a caller using the wrong one and says so rather than implying otherwise. No lower bound on either route: Churchill–Chu's leading 0.68 **is** the conduction limit it approaches as Ra → 0. | Churchill, S. W. & Chu, H. H. S., "Correlating equations for laminar and turbulent free convection from a vertical plate", *International Journal of Heat and Mass Transfer* 18(11) (1975), 1323–1329, as printed in Incropera, DeWitt, Bergman & Lavine, *Fundamentals of Heat and Mass Transfer*, 6th ed. (Wiley, 2007), §9.6.1, Eq. 9.27 (Ra_L ≤ 10⁹); and §7.1 (transition Re) with §7.2, Eq. 7.30 (Re_L ≤ 5×10⁵) |
| `thermal.lumped.first_order_capacity` | `convection_property_range_utilization` | 0.6/Pr under forced convection; **0** under free convection | ≤ 1 (`CORRELATION_RANGE_LIMIT`) | Pr ≥ 0.6 is the restriction Incropera prints with Eq. 7.30: the Pr^(1/3) factor is the constant-property Blasius similarity result, and below 0.6 — liquid metals — the thermal boundary layer is far thicker than the velocity one and the exponent is not 1/3. There is no upper bound. **Churchill–Chu states no Prandtl restriction at all**, which is what its [1+(0.492/Pr)^(9/16)]^(4/9) denominator is for, so the free-convection route has no property range to leave and the utilization is zero. That zero is a statement about the equation being evaluated, not a concession to the caller: reaching it still requires the full free-convection declaration, and such a caller still has to clear the flow range and the agreement ratio. Bound definitional. | Incropera, DeWitt, Bergman & Lavine, 6th ed. (2007), §7.2, printed with Eq. 7.30; Churchill & Chu (1975) for the absence of a Prandtl restriction on the free-convection form |
| `thermal.lumped.first_order_capacity` | `convection_conductance_agreement_ratio` | h_declared/h_correlated, with h_declared = hA/A_s and h_correlated = Nu·k_f/L from whichever correlation the declaration resolves to | ∈ [1/2, 2] (`CONVECTION_AGREEMENT_FACTOR`) — **a convention, not a cited threshold** | Two routes to one coefficient, the same shape as `geometry_route_ratio`, and the reason it matters is that every other condition in this model is computed from hA. **What is established** is that the two legitimately differ: the correlation's own scatter against its data, the gap between an idealized isothermal plate and whatever shape the body is, and the caller's film-temperature property evaluation, which this domain cannot check. **What is not established is the number** — 2 is where the disagreement stops being attributable to those and starts being attributable to a mistake (a coefficient off the wrong chart, a length off the wrong axis, an area counted once instead of twice), and it is also the magnitude at which a wrong coefficient destroys a calculation rather than degrading it. **No source prints it for this comparison and it is recorded here as a convention.** Two-sided: a declared hA far *below* the correlation is the conservative error and far *above* is the dangerous one, and both are bounded because nothing here can tell which route the caller got right. **A disagreement is a finding about the declaration, not the run** — the solve uses the declared hA either way. | Physics cited, number not: Nu = h·L/k_f from Incropera et al., 6th ed. (2007), §6.5; the correlations as above |
