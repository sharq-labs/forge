# v2.0-core-freeze — Core Freeze V2

**Additive.** Core V2 adds `engcore.hybrid_uq`, which provides routed uncertainty quantification:

- a resolved grid;
- a named local-Gaussian / linearized route with mandatory validity diagnostics and multistart;
- a verified rebuilt grid;
- or a refusal.

Nothing in Core V1 changed:

- 194 V1 frozen symbols, digest `c80e6418…`, all byte-identical;
- the V1 freeze verifier still passes.

**V2 surface:** 221 frozen symbols, digest `fd7d3f9f3f7d641e7484df15dd6ae4ac09cf4b072a19f230d6fcfb75fc9f37cb`.

Report: `certification/CORE_FREEZE_V2.md`. Verify:

```
python -m tools.certification.core_freeze_v2 --verify
python -m tools.certification.core_freeze --verify
python -m tools.certification.core_certificate --verify
```
